================================================================
 PASTE THIS WHOLE FILE AS YOUR FIRST MESSAGE IN A NEW CHAT
================================================================

You are continuing development of SYL (Space You Land) — a 100%-realistic,
from-scratch, first-person space game in Unreal Engine, built by Jaron. Before
doing anything else, get oriented IN THIS ORDER:

1. Read SESSION_HANDOFF.md (how to operate: tools, the Blender->FBX->UE asset
   pipeline, screenshot decode trick, Blueprint scripting, git auto-backup,
   gotchas) and BUILDLOG.md (current state, conventions, and the "Next up" list).
   Both are in the project root:
   C:\Users\lilli\Documents\Unreal Projects\CurtisAILab
   NOTE: BUILDLOG.md is the LEAN live log (recent entries + Next up). The full
   history is in BUILDLOG_ARCHIVE.md — do NOT read it at startup; open it only
   for a deep dive into past how/when/who. This keeps session-start cheap.

1b. VERIFY THE LIVE SCENE vs THE LOG (catches crash "ghost" state). Once Unreal
   is connected, do a quick actor enumeration (e.g. SceneTools.find_actors by
   name/class) and compare against what the log says should exist. Flag any
   actor NOT in the log (a ghost left by a crashed/limit-hit session) and any
   logged actor missing from the scene. A prior planet build crashed on a usage
   limit and left an invisible "brown layer" actor no later session knew about,
   because it walked the LOG not the WORLD. Cheap (1-2 calls); do it before new
   work whenever something looks off.

2. Read the project-root MEMORY.md. It is the agent-neutral memory shared through
   GitHub. If your runtime also has private linked memories (realism-law,
   syl-endgame-intent, syl-project-reality, working-style-jaron,
   asset-pipeline-toolchain, syl-drive-docs), load them too, but treat the repo
   handoff files as the state both Claude and Codex can see.

3. Connect to Unreal: use ToolSearch (query "unreal") to load unreal-mcp when
   available, then list_toolsets. The server is at 127.0.0.1:8000. Codex desktop
   can use direct MCP JSON-RPC over local HTTP if the deferred tools are not
   exposed. If the server is down, ask me to open the Unreal project.

4. Then continue from BUILDLOG.md "Next up", honoring the realism law
   (100% real, no fakes; forward-compatible / data-driven / real units), and
   SAVE + COMMIT + PUSH to GitHub after each coherent step.

How things get built here: assets are modeled from scratch in Blender (headless)
-> exported FBX -> imported into Unreal; game logic is scripted with BlueprintTools
write_graph_dsl; everything auto-backs-up to github.com/JaronKBragg7337/SpaceYouLand
(gh is already authenticated). If you don't see a capability, search for it
(ToolSearch / direct MCP / list_toolsets / find_node_types) or build it from scratch before
concluding it doesn't exist.

Claude and Codex share progress through SESSION_HANDOFF.md, BUILDLOG.md, and
MEMORY.md. Every completed chunk must state who built it and be pushed so the
other agent can continue without guessing. The repository is currently PUBLIC,
so never commit secrets or personal data.

CRASH/LIMIT DISCIPLINE (a crashed session is a contamination event):
- COMMIT BEFORE a risky/large change too, not just after — so there's always a
  "before" snapshot to roll back to if a session dies mid-task.
- If you sense you're near a usage limit or about to do something risky, FIRST
  append a one-line "WARNING: SESSION INCOMPLETE — was mid-<X>, scene may have
  an unsaved/uncommitted <Y>" to BUILDLOG.md and push it. One line saves the
  next agent from chasing a ghost for hours.
- After any session that hit a wall or crashed, treat the live Unreal scene as
  UNKNOWN until verified by enumeration (step 1b) — do not trust the log alone.

To run continuously and hands-off, I'll use:  /loop  (you self-pace, do one
coherent chunk per pass, log + screenshot + push each pass).

START NOW by reading SESSION_HANDOFF.md and BUILDLOG.md, confirm Unreal is
connected, then tell me the current state and what's next.
================================================================
